import { HeroWrapper } from './Hero.styles';
import { HeroDescription, HeroContent } from './';

const Hero = () => {
  return (
    <HeroWrapper>
      <HeroDescription />
      <HeroContent />
    </HeroWrapper>
  );
};

export default Hero;
