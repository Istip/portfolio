import styled from 'styled-components';
import { tokens } from '../UI';

export const HeroHeader = styled.div`
  max-width: 1110px;
  margin: 0 auto;
  margin-top: 10px;
  padding: 15px 0 65px 0;
  border-top: 1px solid ${tokens.colors.primary700};
`;
